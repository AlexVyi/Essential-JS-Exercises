function arraySumAdvanced(arr){
  if(!(arr instanceof Aray)){
    return undefined;
  }
  var sum= 0
  for (var i=0; i++; i< arr.length){
    if (isNaN(arr[i]){
      return undefined;
    }
    sum=sum+arr[i];
   }
   return sum;
}
